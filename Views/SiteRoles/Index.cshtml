@model IEnumerable<dotnet_facebook.Models.DatabaseObjects.Roles.SiteRole>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SiteRoleName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsDefault)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AdministrativePerms)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SiteRoleName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsDefault)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdministrativePerms)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SiteRoleId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SiteRoleId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SiteRoleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
