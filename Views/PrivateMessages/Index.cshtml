@model IEnumerable<dotnet_facebook.Models.DatabaseObjects.Users.PrivateMessage>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sender)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Receiver)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Message)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MessageDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Sender.Nickname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Receiver.Nickname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Message)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MessageDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PrivateMessageId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PrivateMessageId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PrivateMessageId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
